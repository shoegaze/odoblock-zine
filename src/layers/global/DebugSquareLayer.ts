import { createLayer, Layer } from "../../Layer"
import debugSquareScene from "./DebugSquareScene"


const debugSquareLayer: Layer = createLayer(debugSquareScene)

export default debugSquareLayer